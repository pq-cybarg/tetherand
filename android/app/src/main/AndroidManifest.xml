<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- M7a threat-detection signal sources -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" tools:targetApi="s"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" tools:targetApi="s"/>
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:label="Tetherand"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <!-- Visible Compose entry point (icon launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Invisible CLI-invocable activity: receives START / STOP intents
             from the host-side `tetherand` binary. -->
        <activity
            android:name=".TetherandActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="dev.tetherand.app.START"/>
                <action android:name="dev.tetherand.app.STOP"/>
            </intent-filter>
        </activity>

        <service
            android:name=".TetherandService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="reverse_tethering"/>
        </service>

        <!-- M7a: standalone threat-detection foreground service.
             Runs alongside the VPN services — Android allows multiple
             foreground services on different subtypes. -->
        <service
            android:name=".threat.service.ThreatDetectionService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="threat_detection"/>
        </service>

        <!-- M3: Privacy Chain VpnService. Parallel to TetherandService;
             only one VpnService can be active at a time and the user
             picks via the UI tab. -->
        <service
            android:name=".service.TetherandChainService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="privacy_chain"/>
        </service>

        <!-- M2: USB Open Accessory receiver. Wakes when the host (Mac)
             switches the Seeker into accessory mode via control transfers. -->
        <service
            android:name=".transport.AoaAccessoryService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED"/>
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED"
                android:resource="@xml/aoa_accessory_filter"/>
        </service>

        <!-- M9: Hardened Mode honeypot. Binds 6 unprivileged TCP ports
             (8080/8443/8000/9000/1080/3128) and logs every inbound
             connection as a High-severity Alert via ThreatDb. Foreground
             service so the OS doesn't kill it while Hardened Mode is
             active during a conference. -->
        <service
            android:name=".hardened.decoy.DecoyListenerService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="honeypot"/>
        </service>

        <!-- M10: AI Guard clipboard scrubber. Foreground because Android
             restricts clipboard reads to foreground apps and default IMEs
             from SDK 30+; as a foreground service we satisfy that. The
             scrubber runs the prompt-injection regex against every clip
             change and surfaces hits as High-severity Alerts via ThreatDb. -->
        <service
            android:name=".aiguard.clipboard.ClipboardScrubberService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="clipboard_scrubber"/>
        </service>

        <!-- M9: Hardened Mode Quick Settings tile ("DEFCON Mode"). One-tap
             entry/exit. Must be exported with BIND_QUICK_SETTINGS_TILE so
             the system SystemUI process can bind to it. -->
        <service
            android:name=".hardened.tile.HardenedTileService"
            android:icon="@drawable/ic_report_problem_24dp"
            android:label="DEFCON Mode"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>
    </application>
</manifest>
